/** Thin re-export — canonical checker lives in authoring for Green reuse. */
export {
  assertLayoutInvariants,
  formatLayoutViolation,
  layoutInvariantViolations,
  type LayoutInvariantViolation,
} from "@/authoring/layoutInvariants";
